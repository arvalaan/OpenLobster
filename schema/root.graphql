# Root schema — scalar y schema directive
# Query, Mutation, Subscription se definen/extienden en los archivos por dominio

scalar JSON

schema {
  query:        Query
  mutation:     Mutation
  subscription: Subscription
}
